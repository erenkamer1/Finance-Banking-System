import React from "react";
import { useNavigate } from "react-router-dom";

const personal_list = () => {
    return (
        <div>
            <h1>Personal List</h1>
        </div>
    );
};

export default personal_list;