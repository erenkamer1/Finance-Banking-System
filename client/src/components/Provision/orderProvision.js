import React from "react";
import { useNavigate } from "react-router-dom";

const orderProvision = () => {
    return (
        <div>
            <h1>orderProvision</h1>
        </div>
    );
};

export default orderProvision;